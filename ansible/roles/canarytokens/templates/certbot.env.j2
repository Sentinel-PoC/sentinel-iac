# Canarytokens certbot.env
# Managed by Ansible — OPS-572
# Env var names verified against upstream certbot.env.dist
# Used by nginx_common_certbot service in docker-compose-letsencrypt.yml variants.
# In Pangolin-proxied deployments, TLS is terminated at Pangolin;
# this env file is generated but the letsencrypt compose variant is NOT used.

MY_DOMAIN_NAME={{ canarytokens_certbot_domain }}
EMAIL_ADDRESS={{ canarytokens_certbot_email }}
