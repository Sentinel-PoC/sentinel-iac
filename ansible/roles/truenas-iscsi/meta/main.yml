---
galaxy_info:
  author: sentinel-worker
  description: >
    TrueNAS SCALE REST API wrapper for repeatable iSCSI provisioning.
    Covers extent creation (zvol- or file-backed), target creation,
    and target-to-extent LUN mapping. Destroy operations are gated
    behind confirm_destroy: true. OPS-1011 (child of OPS-947).
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: any
  galaxy_tags:
    - truenas
    - iscsi
    - storage
    - lun

dependencies: []
