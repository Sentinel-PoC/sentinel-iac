# /etc/sentinel/minio-replicate.env
# Environment variables for minio-replicate.sh
# OPS-42: Managed by Ansible — do not edit manually
# vault_minio_access_key / vault_minio_secret_key: fetched from secret/minio fields access_key / secret_key

MINIO_PRIMARY_URL='{{ minio_replicate_primary_url | default("https://s3.208.haist.farm:9000") }}'
MINIO_REPLICA_URL='{{ minio_replicate_replica_url | default("https://s3-replica.208.haist.farm:9000") }}'
MINIO_AK='{{ vault_minio_access_key | default("CHANGE_ME") }}'
MINIO_SK='{{ vault_minio_secret_key | default("CHANGE_ME") }}'
