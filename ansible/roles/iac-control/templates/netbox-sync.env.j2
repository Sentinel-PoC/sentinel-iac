{# OPS-303: tokens are populated by netbox-sync.yml from Vault and asserted   #}
{# non-empty before this template renders. No defaults — fail loud if absent. #}
NETBOX_API_TOKEN={{ vault_netbox_api_token }}
NETBOX_API_ENDPOINT={{ netbox_api_endpoint }}
PROXMOX_API_TOKEN_ID={{ vault_proxmox_api_token_id }}
PROXMOX_API_TOKEN_SECRET={{ vault_proxmox_api_token_secret }}
