apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OPS-150: ImageDigestMirrorSet — Harbor primary, off-cluster mirror fallback
# Runbook: docs/research-april-power-incident/OPS-147-mw1-registry-split-runbook.md
#
# Apply via: oc apply -k kustomize/idms/
# Triggers MCO node rollout — apply during MW1 maintenance window.

resources:
  - platform-images.yaml
